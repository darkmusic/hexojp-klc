# Database settings
DB_HOST=localhost
DB_PORT=5432
DB_USER=hexojpadmin
DB_NAME=hexojp
DB_PASSWORD=hexojpadmin
POSTGRES_USER=hexojpadmin
POSTGRES_PASSWORD=hexojpadmin
POSTGRES_DB=hexojp
POSTGRES_PGDATA=/data/postgres

# Docker container names
DB_CONTAINER=hexojpdb

# Docker volume names
POSTGRES_VOLUME=postgres

# Container user settings
HEXOJP_USER=hexojp
ROOT_USER=root

# Host paths
DB_FILES_PATH=dockerfiles/db

# Database dump file
DB_DUMP_FILE=hexojp.tar.gz

# Docker compose settings
NETWORK_NAME=hexojp-network

# PostgreSQL configuration
MAX_CONNECTIONS=1000
SHARED_BUFFERS=3GB

# Server settings
SERVER_PORT=4000
